import type { FC } from 'react';
import React from 'react';
import { Nav } from '../index';

export const NavExample: FC = () => <Nav
    title={<h1>UI Examples</h1>}>
    {/* insert links */}
</Nav>;
